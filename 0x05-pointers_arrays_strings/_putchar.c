#include <unistd.h>
#include "main.h"

/**
 * putchar write the character c to stdout
 * @c: the character to print
 * Return: on success 1
 *         on error, -1 is returned. and error
 */
int _putchar(char c)
{
	reurn (write(1, &,1));
}
