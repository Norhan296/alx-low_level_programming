#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	int a = 0;
	char s = 'a';

	while
		(a <= 9) {
		putchar(a + 48);
		a++;
		}
	while
		(s <= 'f') {
		putchar(s);
	        s++;
		}
	putchar('\n');
	return (0);
}
